# Architecture Decision Log

This file is the index of all major architecture and implementation decisions.

Update this file whenever a design decision is made, changed, clarified, or discovered
during implementation. For larger decisions, also create a separate `ADR_NNN_*.md` file.

| ADR | Date | Status | Title | Summary | Files affected |
|---:|---|---|---|---|---|
| 000 | 2026-06-19 | Accepted | Decision log process | The coding agent records architectural decisions in docs/decisions before or alongside code changes. | docs/decisions, requirements.yaml, docs/spec, include |
| 001 | 2026-06-19 | Accepted | Scope: motor-control MCU only; freeze SPI seam | Build only the motor-control MCU; fully specify the inter-MCU SPI link as a frozen boundary contract; network MCU + external protocol are a separate project. | ADR_001, docs/spec/06_spi_protocol.md, include/mc_spi_protocol.h, requirements.yaml |
| 002 | 2026-06-19 | Accepted | Clean-room reimplementation with golden-reference validation | Reimplement modules from the specs; validate each against bldc_axis_controller as a golden reference; verified on-target via SWD live watch (ADR-005). | ADR_002, docs/spec/15_basic_tests.md, tests/, requirements.yaml |
| 003 | 2026-06-19 | Accepted | Velocity feedback: position-tracking observer default | State-estimator default velocity = encoder position-tracking observer (Ellis Fig 18-11); finite-difference+LPF as runtime-selectable fallback. | ADR_003, docs/spec/11_feedback_and_encoders.md, include/mc_state_estimator.h, requirements.yaml |
| 004 | 2026-06-19 | Accepted | Per-board hardware config + generic motor model | Introduce MC_BoardConfig_t and MC_MotorModel_t; hardware values become per-board config; ship board profile 0 from the proven reference board. | ADR_004, docs/spec/17_board_and_motor_config.md, include/mc_board_config.h, include/mc_motor_model.h, requirements.yaml |
| 005 | 2026-06-19 | Accepted | Bring-up & verification via on-target SWD live watch | Primary verification is incremental on-target bring-up over SWD using the debugger live watch window + volatile debug-mirror/inject structs; host Unity tests optional. | ADR_005, docs/spec/15_basic_tests.md, CLAUDE.md, requirements.yaml |
| 046 | 2026-06-28 | Accepted | GUI-fireable open-loop d-axis voltage pulse for plant ID (R/L) | Exposed the existing open-loop align path (ADR-024) over the OD (`0x2900:6/7/8/9` dq_test_voltage_v/angle/enable/dwell_ms, motor-owned, not persisted) so the d-axis voltage step can be fired from the PC tool. Scheduler arbitration gains a 3rd branch (priority watch-inject > OD dq-test > remote); drives the align path open-loop as a **pulse** — hold Vd for dwell_ms, then return to 0 + disarm. Vd clamped ±12 V (MC_C2_VD_MAX raised 3 → 12 V — shared open-loop clamp, so alignment/manual ceilings rise too; accepted since alignment current-regulates to its target, duty saturates beyond ~Vbus/2, OC trip bounds current), dwell clamped to MC_DQ_TEST_MAX_MS (10 s). Motor-level (NOT axis_manager) — CMC forwards the OD writes as transport; commissioning overrides the remote enable (motor live regardless of axis mode, reports ENABLED). GUI: "Plant ID — d-axis voltage pulse" section (Vd/angle/dwell, Fire/Stop, auto-points the DAC at ia). Dead-time caveat (~1.9 µs DTG=200, no comp, ~0.9 V error): L from τ is robust, R needs a V–I sweep (slope=1/R); ship single-pulse (option B), automated sweep is a future enhancement. **Extended (fw_build 65):** `0x2900:10 dq_test_axis` adds q-axis (FOC SVPWM, 90° ahead) and brushed_phase (open-loop H-bridge armature voltage, PI held in reset), so the open-loop tester drives on both the FOC and brushed backends. | ADR_046, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 65), include/mc_od_store.h, gui |
| 003.1 | 2026-06-28 | Accepted | Expose observer output LPF (obs_filter_alpha) + correct the bandwidth label | The observer's output low-pass (`obs_filter_alpha`, mc_state_estimator.c:95) was hardcoded 0.3 and not OD-exposed. Exposed as `0x2500:7 est_obs_filter_alpha` (F32, RW, PERSIST, default 0.3 ≈ 57 Hz @ 1 kHz), synced live to `s_est_cfg.obs_filter_alpha`. Clarified that `velocity_filter_hz` (0x2500:2) shapes ONLY the finite-diff fallback (`velocity_filtered`), not the observer output, when `use_observer=1`. Fixed the GUI estimator bandwidth label (it had mislabeled `velocity_filter_hz` as the observer's LPF; now shows the observer output LPF from `obs_filter_alpha` + notes the finite-diff fallback). Refinement of ADR-003. | ADR-003, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 62), include/mc_od_store.h, src/mc_od.c, gui |
| 047 | 2026-06-28 | Accepted | Stepped-sine current sweep for resonance / FRF identification | New generator `mc_freq_sweep.c/.h` injects `bias + amplitude·sin(2πf t)` as iq, stepping `f` from start→end by step, `dwell_s` each, phase continuous (no click). Generated in the **20 kHz fast loop** (not the 1 kHz signal-gen) for fidelity to ~200 Hz — the 1 kHz medium loop staircases sinusoids (and caused the audible artifact). OD block `0x2920:1-9` (motor-owned, not persisted): start/end/step_hz, dwell_s, bias_a, amplitude_a, enable (RW); current_hz (RO PDO-mappable), active (RO). Fast loop edge-detects enable→Start, auto-stops past end_hz; torque-mode only, OC/limits apply. GUI section under Tuning (Start/Stop + live freq); graph 0x2920:8 vs 0x2310:2 velocity for the FRF. Additive OD, no version bump, CMC unaffected. Validated by tests/test_freq_sweep.c. | ADR_047, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_freq_sweep.h, src/mc_freq_sweep.c, src/mc_scheduler.c (fw_build 63), include/mc_od_store.h, src/mc_od.c, tests/test_freq_sweep.c, gui |
| 048 | 2026-06-28 | Accepted | Current-command notch filter for resonance suppression | New `mc_notch.c/.h` RBJ band-reject biquad on the velocity-loop current command (`s_iq_cmd_published`) at the 1 kHz medium rate — numerically conditioned for a ~55 Hz notch; **NOT** on the current-loop voltage output (the fast loop would fight/wind up against it). OD `0x2930:1-3` (notch_enable / freq_hz / bandwidth_hz, motor-owned, PERSIST); coeffs recomputed off the control path on band change, trig-free `Update`; resets bumplessly on velocity-loop restart; torque/sweep bypass. Default off, 55/30 Hz (~40-70). Adds phase lag → verify loop stability when on. Additive PERSIST OD, no version bump, no wipe. GUI config group in Motor Config. Validated by tests/test_notch.c. | ADR_048, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_notch.h, src/mc_notch.c, src/mc_scheduler.c (fw_build 64), include/mc_od_store.h, src/mc_od.c, tests/test_notch.c, gui |
| 049 | 2026-06-29 | Accepted | Direct brushed current-loop gains (replace the bandwidth derivation) | Brushed armature-current PI gains made directly settable: `0x2400:6/7 hb_cur_kp/ki` RO→RW+PERSIST (scheduler applies them verbatim), `0x2400:8 hb_cur_bandwidth` removed + the R/L→bandwidth derivation (kp=wc·L, ki=wc·R) dropped. Reason: brushed bring-up wants hand-tuned kp/ki + a trusted step response, not a derivation off not-yet-identified R/L. R/L (0x2000:3/4) still feed the model; default 5.55/6300 (ADR-039 bootstrap). Contract: 0x2400:6/7 RO→RW, 0x2400:8 removed — non-PDO, no version bump, CMC unaffected; old persisted 0x2400:8 ignored on restore (MC_Od_Write→NO_OBJECT, (void)). GUI: editable kp/ki rows (bandwidth field gone); bw estimate now hb_cur_kp/L. | ADR_049, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 66), include/mc_od_store.h, src/mc_od.c, gui, smoke_test.py |
| 050 | 2026-06-29 | Accepted | Quadrature encoder count exposed (TIM2) | TIM2 quad encoders (PA15/PB3) were CubeMX-configured (TI12 4x, 32-bit free-run, filtered) but never started; added HAL_TIM_Encoder_Start in main.c USER CODE 2 + a boundary accessor (mc_quad_encoder_stm32g474.c, MC_QuadEnc_Count = (int32_t)TIM2->CNT) so the HAL-free scheduler mirrors the count to 0x2510:4 (I32, RO, PDO) each medium cycle for a manual/graph read. Raw diagnostic count for bring-up; NOT yet the position-feedback source (wrap/scale/direction + routing through the feedback interface deferred). Additive OD, no version bump, CMC unaffected. | ADR_050, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_quad_encoder.h, src/mc_quad_encoder_stm32g474.c, src/mc_scheduler.c (fw_build 67), include/mc_od_store.h, Core/Src/main.c, gui |
| 051 | 2026-06-30 | Accepted | Fail-safe when the persistent config doesn't load | Boot load now retries (MC_STORE_LOAD_ATTEMPTS=3) and, if no valid record loads (MC_PersistentStore_HasValid false), (a) inhibits the operational drive via fs.severe_active (mode manager won't enable; commissioning/align bypass it) and (b) raises 0x2600:1 fault_flags bit MC_IF_FAULT_NO_CONFIG (self-clears on a valid load/save). Closes the silent-fallback-to-defaults gap (default electrical offset 0 -> mis-commutation at cold boot). The store read is already A/B-redundant + CRC + version-gated + flash-ready-at-init, so the fix is fail-safe behaviour, not the read. Bit-definition contract change, no version bump, CMC unaffected. Separate from a possible position-estimator init-order race (still open in the cold-boot note). | ADR_051, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 68) |
| 052 | 2026-06-30 | Accepted | Quad encoder -> estimator (close the brushed velocity loop) | The medium loop builds an MC_PositionSensorSample_t from the TIM2 quad count (position_rad = count * 2pi/quad_counts_per_rev, 0x2500:8 signed; absolute=false) and feeds the SAME MC_StateEstimator_Update -> the brushed velocity loop closes with the velocity controller + 0x2300 gains reused unchanged (encoder-agnostic). Estimator accumulates wrap_pi deltas so the continuous quad needs no anchor; the ADR-037/038 startup anchor is now gated on sample.absolute (SSI keeps it, quad skips it). Encoder source tied to backend for now (INTERIM: brushed->quad, FOC->SSI); clean per-board selector is next. Velocity works immediately; absolute position/soft-limits still need homing. New 0x2500:8 quad_counts_per_rev (F32 RW PERSIST, default 4000), additive, no version bump, CMC unaffected, persist 376/448 B. | ADR_052, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 69), include/mc_od_store.h, src/mc_od.c |
| 053 | 2026-06-30 | Accepted | Frequency sweep overlays both backends | The ADR-047 sweep generation + injection lived entirely in the FOC fast-loop branch, so it never ran on brushed (different dispatch branch; the sample only fed the FOC cmd.iq_a). Hoisted the freq_sweep_enable edge-detect + the single MC_FreqSweep_Sample above the backend dispatch (sweep_on/sweep_iq) and overlaid on BOTH the brushed i_cmd and the FOC cmd.iq_a; sampled once/cycle, torque-mode only, inert during hb_test. With the quad velocity (ADR-052) the brushed sweep->velocity-response->Bode workflow now works. No OD/contract change. | ADR_047, src/mc_scheduler.c (fw_build 70) |
| 054 | 2026-06-30 | Accepted | Holding-current release on settle | New 0x2300:9 holding_enable (U8 RW PERSIST, default 1) -- a BOOLEAN, not a current (the PI provides whatever current is needed; this just toggles holding on/off). 0 = release: in velocity mode, once vdem~0 AND |vact| < 0.1 rad/s for 1000 ms, force s_iq_cmd_published=0 + reset the velocity controller (parks the integrator, no drift-windup); latched released until a non-zero command (no drift-hunting); bumpless resume. 1 = always hold. (Revised from F32 holding_current_a -> U8 holding_enable, build 73.) Refines the user's sketch: gate on actual velocity (not just target), park the integrator (not just the output), latch until a real move. Velocity-mode only, backend-agnostic (velocity-loop output). SAFETY: drifts if back-drivable -- only for self-locking axes. Settle eps + delay hardcoded. Additive OD, no version bump, CMC unaffected, persist 381/448 B. | ADR_054, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 73), include/mc_od_store.h, src/mc_od.c, gui |
| 055 | 2026-06-30 | Accepted | Backend-relevant motor-config gray-out (PC tool) | GUI-only: Motor Config grays out (disables) value fields not used by the selected backend (combo 0x2000:6), live on selection + after read-back. Conservative classification: FOC-only = 0x2400:1-5 + 0x2000:5 (poles) + 0x2700:3/4 (electrical align); brushed-only = 0x2400:6/7 + 0x2500:8 (quad counts/rev); everything else shared/editable. Field disabled, label stays enabled with a why-tooltip. Also added the 0x2500:8 quad_counts_per_rev row (in the contract since ADR-052 but missing from the GUI). No contract change, no version bump; lives in the Lightweight_CMC GUI. | ADR_055, gui (Lightweight_CMC) |
| 056 | 2026-06-30 | Accepted | Abandon the position trajectory on leaving position mode | A completed plan keeps active=true (only HALT cleared it), so MC_Trajectory_Evaluate keeps returning valid@old-target; after a velocity-mode joystick trim, returning to position mode re-used the stale plan and drove BACK to the old target. Fix: on leaving position mode (D3 else, transition out) set s_traj.active=false -> inactive plan returns valid=false -> re-entry holds s_pos_hold_rad (re-latched to the CURRENT/trimmed position). Normal completed-move hold unchanged (never leaves position mode). Fresh move still needs a new setpoint. CMC caveat: motor obeys an explicit NEW_SETPOINT to the old target -- CMC should return to position mode without re-asserting it. On-target reporting (build 74): the abandoned/manual hold is NOT a commanded target, so TARGET_REACHED/on-shot must not fire there; cascade now tracks at_cmd_target (true only for a genuine completed active plan, false in the no-active-plan hold) and reached = complete && at_cmd_target && perr-small -> after a trim the motor stops reporting on-target/on-shot to the CMC. Motor-only, no OD/contract change, no version bump. | ADR_056, src/mc_scheduler.c (fw_build 74) |
| 057 | 2026-07-01 | Accepted | PC-triggered hard-stop homing / encoder zeroing | Incremental (quad) encoders have no absolute zero at power-on; added a drive-to-hard-stop homing routine. home_command=1 (0x2700:8) drives velocity mode at home_velocity_rad_s (0x2700:6, signed) until |armature current| > home_current_a (0x2700:7) for MC_HOME_DWELL_MS (10 ms) OR the OC trip fires (a hard stop spikes past the OC limit before a long dwell confirms, and the trip blocks the drive, so it counts as the stop; an OC hit is consumed/cleared so the axis stays usable) -> the stall -> capture position as the encoder zero (s_home_offset_rad, like set-mech-zero, auto-saved) + stop -> home_status (0x2700:9) DONE; MC_HOME_TIMEOUT_MS (30 s) -> FAILED; command 0 aborts/clears. Zero = the stop (no back-off). Uses the velocity loop (needs quad feedback 0x2500:8); push bounded by the velocity current limit (0x2300:4). Preempted by inject/dq-test; bypasses the CMC while running. New OD 0x2700:6-9 + MC_IF_HOME_* enums; additive non-PDO, no version bump, persist 397/448 B. GUI: "Home to end stop" section (velocity/current, Home/Stop, live status), disabled on FOC. Auto-home-at-startup deferred (same routine). Position recalls gated (build 77): a non-persisted s_homed (false each boot; set by homing DONE or set-mech-zero) blocks a PROFILE_POSITION NEW_SETPOINT unless the encoder is absolute (SSI) or s_homed; 0x2600:1 bit MC_IF_FAULT_NOT_HOMED set while incremental+un-homed, so the CMC homes first (re-home every power-up). Velocity/jog/homing unaffected. Approach velocity ramped (build 78): the homing drive uses vel_slew_limit (0x2300:6/7/8 accel/jerk limits, ADR-042), reset from the current velocity on start, so it accelerates into the approach instead of stepping. Default home_velocity_rad_s changed -1.0 -> -0.3 rad/s (build 84); it was already PERSIST (0x2700:6), so a set value survives power cycles after a flash save -- only the factory default changed. Zero-at-stop + back-off (build 86): on finding the stop the routine captures the zero AT the hard stop (the datum, position 0), then drives the OPPOSITE direction (ramped, -home_velocity) for MC_HOME_BACKOFF_MS (1000 ms) and finishes -- the axis is left parked clear of the stop. When position mode re-engages, the hold-on-enable (s_pos_hold_rad = current position) holds it at the backed-off spot, so it does NOT drive 0 back into the stop (which would fight it). Two-phase RUNNING (approach -> back-off); the 30 s timeout is checked only during approach. No-movement detection (build 80): the current dwell (home_current_a 0x2700:7) is replaced by a no-movement stall -- after the axis has moved (|v| > MC_HOME_STILL_EPS 0.01 rad/s, armed so the ramp-up from rest isn't mistaken for the stop), |v| < 0.01 for MC_HOME_STILL_MS (1000 ms) OR the OC trip -> DONE; any stale s_oc_trip is cleared at homing start so a latched trip can't instant-complete; home_current_a deprecated (unused, kept to avoid a layout/version change) + PC-tool stall-current field removed. | ADR_057, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 81), include/mc_od_store.h, src/mc_od.c, gui |
| 058 | 2026-07-01 | Accepted | Diagnostics panel -- faults, history, operating state | Motor: OC trip becomes a fault_flags bit (MC_IF_FAULT_OVERCURRENT) so fault_flags is the full fault register; new fault_flags_latched (0x2600:10, U32 RO) = sticky OR of fault_flags since boot (fault history, not persisted). PC tool: "Diagnostics (faults & state)" group on the Motor Command tab, polled 1 Hz -- Motor {state (statusword), active faults, fault history} + CMC {state (axis_state), error (0x3004/5), auto-clear count (0x3014)} + a Clear-faults button. GUI routing fix: home_status (0x2700:9) added to _MCFG_READOUT_KEYS so its readout updates -- was stuck at RUNNING (only keys in the routing sets reach _cmd_on_state_read). Additive OD, no version bump, persist unchanged (397/448 B). Per-fault counts (build 87): fault_count_no_config/_not_homed/_overcurrent (0x2600:11/12/13, U16 RO, since-boot, saturating) count each fault's RISING edges -- how many times each has triggered; GUI Diagnostics adds a fault-counts row + a Refresh (manual read) button. | ADR_058, ../Generic_axis_controller/Generic_axis_controller/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 87), include/mc_od_store.h, gui |
| 059 | 2026-07-02 | Accepted | PC-tool config snapshot (save/load to file) | GUI-only: two Setup-tab buttons. "Save config -> file" reads every RW+PERSIST OD entry (motor 0x2xxx + CMC 0x3xxx) from the connected device and writes a JSON snapshot {key:{name,raw}} to the PC -- saves the on-wire raw value so it round-trips exactly regardless of scaling (async reads collected in _on_read_done, 8 s backstop). "Load config <- file" parses the JSON and write_async()es each raw value to the device (RAM), skipping keys not writable / not in this build's OD; operator then clicks Save to flash to commit. Enables off-device backup, post-reflash restore, unit-to-unit cloning. No contract change, no version bump, no motor change. | ADR_059, gui (Lightweight_CMC) |
| 060 | 2026-07-03 | Accepted | Mechanical-zero centering (travel midpoint) | Single-turn SSI loses the turn count at power-off; the startup anchor wraps to within +/-half a turn of the mech zero, so parking near the +/-pi seam flips the position a full turn ("sometimes negative", ADR-038). Fix for a sub-1-turn axis: centre the zero on the travel midpoint so the seam falls beyond the ends. Motor: new cal command MC_IF_CAL_SET_MECH_ZERO_AT (0x2700:1=4) sets s_home_offset_rad = mech_zero_set_rad (0x2700:10, F32 RW transient) -- home set to a COMPUTED value without driving there; auto-saved + s_homed. PC tool: "Centre mech zero" row -- Capture CCW/CW read the absolute mech position (0x2510:1) at each extreme, "Set zero = midpoint" writes (ccw+cw)/2 to 0x2700:10 then fires cal 4. Same absolute frame as the capture -> exact, no movement. Additive OD (non-PDO), no version bump, not persisted. Multi-turn axis still needs homing/multi-turn encoder. | ADR_060, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 82), include/mc_od_store.h, src/mc_od.c, gui |
| 061 | 2026-07-03 | Accepted | Adopt shared contract v5 (dual-bootloader Phase 1) | Contract went to v5 (wire-breaking): MC_IF_PROTOCOL_VERSION 4->5; bootloader OD 0x1F5x + owner MC_IF_OWNER_BOOTLOADER + node MC_IF_NODE_BOOTLOADER(0x07) + msgs 0x14/0x15/0x16 + result codes 0x09-0x0C. A v5 CMC BAD_VERSIONs a v4 motor -> SPI OD link dead until the motor rebuilds. Motor app adopts v5 (REQ-0015 items 1-3): added OD_ROW_MC_IF_OWNER_BOOTLOADER skip in mc_od.c (the X-macro otherwise won't compile against v5 -- undefined owner row for the 0x1F5x entries), repointed .cproject + source-comment include path from ../Lightweight_CMC/Interface to the moved canonical ../Generic_axis_controller/Generic_axis_controller/Interface; default msg dispatch already returns ERR_UNKNOWN_MSG for the download msgs; node-state/result-code enums are additive. Host-compiles clean vs v5. Phase 2 (motor bootloader binary) deferred. | ADR_061, src/mc_od.c, .cproject, src+include comment paths, src/mc_scheduler.c (fw_build 83) |
| 062 | 2026-07-07 | Accepted | Position-integrated jog (parallel to velocity jog) | Joystick jog runs PROFILE_VELOCITY (direct velocity loop) -> no position demand -> following-error / soft-limit / stiff-hold protection don't apply while jogging (the hand-driven mode has the least protection). Added a PARALLEL position-integrated jog, selectable per axis: new OD jog_position_mode (0x2300:10, U8 RW PERSIST) 0 = direct velocity (default, unchanged) / 1 = position-integrated. Fork at the PROFILE_VELOCITY handler: when 1, integrate the ramped velocity into the position hold target (s_pos_hold_rad += vel*dt), run the position cascade (s_traj inactive), leash the reference to the actual (MC_JOG_LEASH_RAD 1 rad, can't run away from a stuck axis) + clamp to soft limits; D3 hold-on-enable latches s_pos_hold_rad = actual on entry. CMC/contract/PDO unchanged (motor-side fork; still PROFILE_VELOCITY on the wire). Reuses the position cascade -- no second control stack; old path untouched. Additive OD, no version bump, default 0 = no change until flipped. Follow-up: the following-error FAULT (bit + tunable window/timeout) still unwired; the leash uses a fixed define for now. | ADR_062, ../Generic_axis_controller/Generic_axis_controller/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c (fw_build 88), include/mc_od_store.h, src/mc_od.c, gui |
| 063 | 2026-07-07 | Accepted | PC-tool GUI restructure | GUI-only (Generic_axis_controller/.../gui), no contract/motor change. (1) Diagnostics (faults & state) moved out of the Motor Command tab into a PERSISTENT panel below the connection bar -- always visible on every tab. (2) Motor Config split into 4 sub-tabs: Motor characteristics (drive backend, motor model), Control loops (pos/vel/current gains, state estimator, notch, live current), Limits & safety (faults/limits, trajectory profile), Calibration (homing, alignment params, cal actions incl. mech-zero centering). (3) New Tools parent tab: tuning signal-gen + frequency sweep (re-parented from Motor Command) + plant-ID open-loop pulse + debug DAC (re-parented from Motor Config) + inertia estimator. Groups routed by title map; all self.* widgets re-parented (no rebuild of the builders). Verified: window builds, tabs/sub-tabs correct, groups in the right sub-tabs, config rows populated. | GUI (Generic_axis_controller) |
| 045 | 2026-06-26 | Accepted | Jerk-limited S-curve trajectory planner (parallel, selectable) | The trapezoidal planner (ADR-025) works and must stay untouched, but its interface was already S-curve-ready (`MC_TrajLimits.max_jerk`, per-segment jerk `a(t)=accel+jerk·t`, 7 segments, `TIME_STRETCHED`, and `traj_integrate` already integrates the cubic jerk term). Added a **parallel** planning fn `MC_TrajScurve_Plan` (new `mc_traj_scurve.c/.h`) filling the same `MC_TrajectoryPlanner_t`; sampling reuses `MC_Trajectory_Update`. **Fixed jerk** (`0x2600:8 max_jerk_rad_s3`); peak v/a solved per move via the single free knob V (peak velocity): `a_peak=min(a_lim,√(jV))`, `T(V)=t_acc+D/V` monotonic → bisect for the requested time, clamp to v_max/distance, **extend time** (`TIME_STRETCHED`) if too short. Rest-to-rest only. Selected by `0x2600:9 traj_use_scurve` (PERSIST, default 0 = trapezoidal); the scheduler branches at the PROFILE_POSITION move-start, both planners feed the shared sampler. Validated by `tests/test_traj_scurve.c` (lands at target at rest, within v/a/jerk, hits requested time / stretches, no overshoot). Additive non-PDO OD → no version bump; **CMC unaffected** (its OD bridge forwards motor entries blindly). GUI: a "Trajectory profile" section in Motor Config. | ADR_045, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_traj_scurve.h, src/mc_traj_scurve.c, src/mc_scheduler.c (fw_build 57), include/mc_od_store.h, src/mc_od.c, tests/test_traj_scurve.c, gui |
| 044 | 2026-06-26 | Accepted | Persistent OD blob was truncating (256→448 B) | The flash payload (`MC_Params_t`, ADR-023) serializes every PERSIST OD entry as `{index,sub,len,value}` (8 B/F32) into a fixed `MC_PARAMS_OD_BLOB_MAX` buffer; `MC_Od_GatherPersistent` **silently break​s when full**. The motor PERSIST set grew to **318 B (41 entries) > 256** → the last 8 motor-owned entries were dropped: **`0x2600:6/7` (soft position limits), `0x2700:3/4` (cal-align), `0x6081-5` (CiA profile)** — written + "saved" but gone after a power-cycle (adding `0x2300:8` this session was the tipping point). **Not a mirror trap** (nothing writes `g_od.pos_limit_*` but the OD path + the `LoadDefaults` seed). Fix: **blob 256→448 B** (fits 318 + 130 headroom; `sizeof(MC_Params_t)`~476 ≤ `MAX_PAYLOAD` 512, so no store buffers grow); **`MC_PARAM_STORE_VERSION` 2→3** so old records are rejected (→ defaults → one re-save). Motor-internal: no contract/version bump, no CMC impact. Follow-up: a runtime truncation flag → `g_mc_debug` so a future overflow is visible. | ADR_044, include/mc_calib_data.h, include/mc_persistent_store.h, src/mc_scheduler.c (fw_build 56), docs/spec/14_persistence.md |
| 043 | 2026-06-26 | Accepted | Soft position-limit deceleration + home-gate | ADR-040 added motor-owned soft position limits (`0x2600:6/7`, home-rel, manually set) but velocity/joystick mode **hard-zeroed** the demand at a limit → slammed + overshot by the stopping distance, and they were enforced even before the mechanical zero was set (meaningless reference). Decision: (1) **decelerate to the limit** — taper the velocity demand to `v ≤ √(2·max_accel·dist_to_limit)` so it glides to a stop AT the limit (no slam/overshoot); decel budget = the envelope `max_accel` (`0x2600:5`), hard-stop fallback when 0. Position mode unchanged (target clamp + planner already decelerate). (2) **Gate on the mechanical zero** — all three sites (target clamp, velocity taper, `AT_LIMIT_LO/HI` bits) active only when `s_home_offset_rad != 0` (the `MC_IF_CAL_DONE_MECH_ZERO` signal); not zeroed → limits off. (3) **Always allow escape** — restrict only motion TOWARD the near limit; motion away is never limited, so you can drive out of the zone (position mode escapes via the target clamp). (4) Torque mode unguarded. New `pos_limits_active()` helper. Validated numerically (glide to limit sub-µrad overshoot; escape unrestricted). **No new OD entries / no contract change** (entries exist) — behaviour refined, CHANGELOG description touched, no version bump. GUI: "capture current position" Lo←pos/Hi←pos buttons. | ADR_043, src/mc_scheduler.c, docs/spec/12_faults.md, ../Lightweight_CMC/Interface/CHANGELOG.md + gui |
| 042 | 2026-06-26 | Accepted | Velocity-demand acceleration ramp, jerk-limited rise (joystick smoothing) | The joystick reaches the motor as a cyclic `velocity_setpoint` in PROFILE_VELOCITY (ADR-021), streamed in coarse ~25 ms steps → stepped/"crunchy" motion. Added a **velocity-demand acceleration ramp** in front of the velocity loop, **only on the PROFILE_VELOCITY demand** (position cascade + tuning generator bypass): slew the demand under an acceleration cap, where the acceleration itself is **jerk-limited on the way UP to the cap but falls FREELY on the way down**. Caps `vel_accel_up`/`vel_accel_dn` [rad/s²] (speeding up / slowing down, 0 = phase off) + shared `vel_accel_jerk` [rad/s³] ramp-up rate (0 = step) — OD `0x2300:6/7/8` (F32 RW PERSIST, default 0). **Asymmetry = stable**: the free fall lets the acceleration drop to track the demand to zero as the velocity lands → no overshoot; the jerk-limited rise only shapes the start → eased starts, snappy stops. Validated numerically (step/stop/reversal zero overshoot; jerk=0 reproduces the plain ramp). **Rejected a symmetric one-sample jerk clamp first** (built + tested): unstable — the acceleration integrates up, peaks as the velocity arrives, carries it past → slow limit cycle. Bump-free; joystick stays a CMC concept (no new motor mode). GUI: 3 fields + Bypass next to the joystick + velocity config group. Additive non-PDO → no version bump; CHANGELOG [4.5.0]. | ADR_042, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_od_store.h, src/mc_od.c, src/mc_scheduler.c, gui |
| 041 | 2026-06-26 | Accepted | Audit GUI config pages for mirror traps; promote estimator observer gains | User asked to ensure no editable Motor-Config row is silently overwritten (the R/L class of bug). Audited `od_mirror_live`'s writes vs `_MOTORCFG_GROUPS`: the **State estimator group** was trapped — `est_obs_kp/ki/kv` + `est_use_observer` (`0x2500:3-6`) were applied from the watch-window `g_mc_inject` and mirrored back to the OD (GUI writes overwritten AND never applied); `est_electrical_offset` (`0x2500:1`) is a calibration RESULT mirrored for display. Fix: **promote the observer gains** — the medium loop now applies them from `g_od.est_obs_*` (OD, GUI-settable + PERSIST, already seeded in mc_od.c), the reverse mirror is removed, and the dead `g_mc_inject.obs_*`/`use_finite_diff_velocity` fields are deleted. `est_electrical_offset` removed from the editable group (cal result; shown via cal status). `est_velocity_filter_hz` (`0x2500:2`) was already correct (applied at line 207). No contract-layout change (entries were already RW PERSIST); behaviour fix + GUI. All other config groups audited clean (R/L already fixed; profile params are CiA-402, a separate group). | ADR_000 (log-only), src/mc_scheduler.c, include/mc_debug.h, gui/mc_gui/main_window.py, ../Lightweight_CMC/Interface/CHANGELOG.md |
| 040 | 2026-06-26 | Accepted | Motor owns + enforces the motion envelope; CMC requests within it | Axis velocity/accel/position limits were CMC-owned (`0x3030/3` → mirrored to motor `0x6081/3/4`; position `0x3031/2` CMC-only), conflating the hardware/safety **envelope** (max the motor can safely sustain) with the per-move **request**. The motor held no independent ceiling — it trusted the CMC. Decision: **split by role** — the motor owns + ENFORCES the envelope (new `0x2600:4 max_velocity_rad_s`, `0x2600:5 max_accel_rad_s2`, F32 RW PERSIST, default 0=disabled), clamping every trajectory + the velocity demand `s_eff_vel_cmd`→`vdem` to it regardless of source (CMC/operator/signal-gen); the CMC owns the per-move **request** (`profile_velocity/accel`, now "the request, clamped", not a naked mirror). Enforcement = velocity + accel ceilings + **manually-set soft position limits** (`0x2600:6/7`, home-rel, **decoupled from homing**) that clamp move targets + stop the velocity demand + set `AT_LIMIT_LO/HI`; CMC `0x3031/2` now redundant (advisory, retire later). Additive non-PDO → no version bump; CHANGELOG [4.5.0]. | ADR_040, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_od_store.h, src/mc_od.c, src/mc_scheduler.c, gui |
| 039 | 2026-06-25 | Accepted (approach) | Brushed-DC / H-bridge drive backend (board profile 1) — same board, two legs as an H-bridge | Adapting the agnostic framework for a brushed-DC actuator (quadrature encoder, end-stops) on the **same board/.ioc**. The 3-phase inverter's legs A+B form the H-bridge (motor across PHA/PHB outputs); leg C unused/forced safe-off; same half-bridge circuit → same current scaling. The brushed backend was reserved up front (`MC_MOTOR_BACKEND_BRUSHED_DC_HBRIDGE=1`, motor-model `pole_pairs`/`phase_count` "1 for brushed", sensor `absolute` flag, `MC_CAL_HOMING`). Decisions: (1) **board profile 1** = A+B H-bridge, leg C off, .ioc reused (each leg already complementary+dead-time+MOE). (2) **Current sense**: repoint ADC2 IN6(`I_C`)→IN7(`I_B`) — pin already analog, one-channel change — so the dual regular-simultaneous capture reads `I_A`+`I_B` at one instant; series armature → `i_armature=(I_A−I_B)/2` (common-mode/offset rejection), `|I_A+I_B|≈0` as a shunt-health check; drop the 3-phase `-(ia+ic)` reconstruction. (3) **Backend dispatch** on `backend_type` at the current→PWM seam (fast loop): FOC path unchanged, brushed path = single armature-current loop → H-bridge modulator → 2-leg duty, no electrical angle/commutation. (4) Outer cascade (trajectory/pos/vel/`mc_current_request` torque→i via Kt) + comms/OD/persistence **reused unchanged**. (5) OC trip on `|i_armature|`. (6) Motor-internal → **no contract change/no version bump**; FOC OD entries (`0x2400`,`0x2500:1`) become N/A (backend-gate). OPEN (per-stage/own ADR): seam shape (vtable vs switch); modulation (sign-magnitude vs locked-antiphase) — raised before coding; current vs voltage mode; **quadrature encoder+estimator** (needs .ioc encoder-mode; ADR-037/038 anchor N/A); **bottom-stroke homing** (`MC_CAL_HOMING`)+soft limits (the reserved `AT_LIMIT` bits). Staged on branch `brushed-dc-backend`. | ADR_039, src/mc_board_config.*, src/mc_current_sense_stm32g474.c, src/mc_scheduler.c, new mc_drive_backend/mc_hbridge, Core/Src/adc.c+.ioc, requirements.yaml, docs/spec/17 |
| 038 | 2026-06-25 | Accepted | Harden the startup anchor against the cold-boot home-load race | ADR-037's one-shot seed worked on a soft/debugger reset but a user hit a residual bug **only on a true cold boot** (unplug): rotor at an end-stop, mech zero at the arc midpoint `−3.138`, move to `−3.0306`, power-cycle → `0x6064` = **`+3.2527`** (`= −3.0306 + 2π`, one turn off, **> π** which the wrap can never output) — yet the zero point still read 0 (so home *was* persisted). Exact fingerprint of **the seed running while `s_home_offset_rad` was still 0**: anchoring the wrap to the encoder zero cancels only at home and is ~1 turn off elsewhere; a soft reset hid it because RAM kept `s_pos_seeded`+continuous so the seed never re-ran. A one-shot that depends on the home-load *ordering* is too fragile (stale build / encoder-ready timing / future reorder defeat it silently). Fix: **continuously self-correcting anchor** — while the drive has never been enabled (`!s_pos_locked`) re-anchor `continuous = home + wrap_pi(single − home)` **every** medium cycle (idempotent once correct, self-corrects if home loads late); **lock on first enable** (`s_eff_drive`) so motion tracks true multi-turn; once locked, stays locked (a later disable won't re-anchor). The wrong↔right anchor differ by whole turns only, so fractional position/velocity/observer are undisturbed. Added **`g_mc_debug.fw_build` (=38)** as a flashed-image marker (the "is it really flashed?" question recurred all through this). Motor-only, no contract change, no version bump. | ADR_038, src/mc_scheduler.c, include/mc_debug.h, docs/spec/11_feedback_and_encoders.md |
| 037 | 2026-06-25 | Accepted | Startup nearest-turn position seed (single-turn encoder boundary) | User bug: start the rotor at certain positions and it reports ~1 turn of phantom travel near "a boundary". Root cause = single-turn absolute SSI encoder; on startup `continuous = single` and the home-relative `mech − home_offset` is computed **unwrapped**, so a start position on the far side of the 0/2π encoder seam from home reads ~2π off. Golden ref (`bldc_axis_controller/alignment.c:238-252`) solves it by wrapping `raw − stored_raw_zero` to ±CPR/2 before seeding; the new project was missing that wrap. Fix: on the **first valid sample**, `MC_StateEstimator_SeedContinuous(home_offset + wrap_pi(single − home_offset))` → startup home-rel ∈ [−π,π] (nearest turn); deltas accumulate as before. One-shot (`s_pos_seeded`). Assumes axis within ±½ turn of home at startup (inherent to a single-turn encoder). Host-test: seam start 6.20→−0.083 rad. Motor-only, no contract change. | ADR_037, include/mc_state_estimator.{h,c}, src/mc_scheduler.c |
| 036 | 2026-06-25 | Accepted | store_status bitfield + save-pending visibility | Diagnosed reported bug: set `0x6081`, Save, reboot → reverts to default. Root cause = a save commits to flash only with the **power stage OFF** (ADR-010; a ~20-40ms flash erase would freeze the FOC loop + over-current trip), so a save issued while the drive is enabled (CMC streams ENABLE) latches but never commits — and nothing reported it (`store_status` was only HasValid 0/1; `store_save_pending` was watch-only). Verified the saved blob is NOT truncated (gather = 249/256 B, 0x6081-0x6085 all present). Fix = visibility, not removing the gate: **`store_status` (0x2800:2) → bitfield `MC_IF_STORE_VALID(0x1) | MC_IF_STORE_PENDING(0x2)`**; GUI "Save to flash" polls it and shows "SAVE PENDING — disable the drive to commit" then "Saved to flash". Backward-compatible (test `& VALID`). Additive defines, no version bump. CHANGELOG [4.2.0]. | ADR_036, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, src/mc_scheduler.c, gui/mc_gui/main_window.py |
| 035 | 2026-06-25 | Accepted | HALT — controlled hold (stay enabled, hold position) | Only quick-stop was wired (clears CMC enable_latch → needs explicit re-enable). `CW_HALT` (0x0100, "controlled stop, hold position") + the CMC's HOLD op-mode were sent but the motor treated HALT as **disable** (operation_enabled=false → coast), so HOLD didn't hold. Implemented HALT (Option 1, pin-and-hold): mode manager HALT+enable → `MC_MODE_POSITION_HOLD` (stays ENABLED); scheduler holds at the position **captured on the HALT rising edge** (`s_traj.active=false` to abandon the move, `s_pos_hold_rad=current`, reset pos ctrl), D3 holds pure-feedback. New setpoints ignored during HALT; resume by clearing HALT (trajectory inactive → holds until fresh NEW_SETPOINT). **Stays enabled → no re-enable needed** (vs quick-stop). No profiled decel (Option 2 deferred: 0x6084/0x6085). No contract change (CW_HALT + MC_MODE_POSITION_HOLD already exist) → no version bump; motor-only — CMC's HOLD now holds as documented. | ADR_035, src/mc_mode_manager.c, src/mc_scheduler.c, docs/spec/09_control_loops.md |
| 034 | 2026-06-25 | Accepted | vel_load_factor — operator load multiplier on velocity-loop kp/ki (REQ-0014) | REQ-0014 (CMC→motor): the CMC web has a load-factor slider (0.3 light → 2.0 heavy) to scale the velocity loop for the payload; was a dead CMC-only placeholder (`0x3040 axis_payload_weight_kg`, [3.6.0]). Added motor-owned **`vel_load_factor` (0x2300:5, F32 RW PERSIST, default 1.0)**, applied in `od_apply_gains`: `s_vel_cfg.pid.kp = vel_kp·factor`, `.ki = vel_ki·factor` (kd unchanged), `factor = clamp(vel_load_factor, 0.3, 2.0)`. Base `vel_kp/ki` (0x2300:1/2) stay the operator baseline; 1.0 = unchanged. Clamp-not-reject (like velocity_ff_gain / current_trip floor); seeded 1.0 in LoadDefaults. Additive non-PDO → version stays 4. CHANGELOG [4.1.0] (also records the CMC-side `0x3040` removal — one migration). | ADR_034, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md + REQUESTS.md, include/mc_od_store.h, src/mc_od.c, src/mc_scheduler.c, docs/spec/09_control_loops.md |
| 033 | 2026-06-24 | Accepted | Cyclic header v4 — position_actual + movement_status (REQ-0013, FIRST wire-break) | REQ-0013 (CMC→motor): the CMC shot-store needs live `position_actual` + a moving/on-target indicator that is **always present** (independent of the host-configurable `0x2A00` telemetry map). Extended the fixed `MC_IfCyclicStatusHeader_t` with **`int32_t position_actual_scaled` (0x6064) + `uint16_t movement_status`** → header 12→18 B, blob budget 40→34, **`MC_IF_PROTOCOL_VERSION` 3→4 (first bump; coordinated motor+CMC+GUI flash)**. `movement_status` trimmed (user) to **MOVING (0x0001, live), ON_TARGET (0x0002, live), AT_LIMIT_LO/HI (0x0010/0x0020, reserved 0 — no soft limits yet)**; dropped REQ-0013's SETPOINT_ACCEPTED/COMPLETE (0x0004/0x0008 reserved). Seam: scheduler computes `movement_status` in `od_mirror_live` and **pushes** via `MC_Comms_SetMovementStatus`; `build_telemetry` reads 0x6064 via OD + stamps the pushed status (header-only field). CHANGELOG [4.0.0]; REQ-0013 annotated with the reduced set. | ADR_033, ../Lightweight_CMC/Interface/mc_if_protocol.h + CHANGELOG.md + REQUESTS.md, src/mc_comms.{c,h}, src/mc_scheduler.c, include/mc_debug.h, gui |
| 032 | 2026-06-24 | Accepted | Acceleration-limited tuning profiles (trapezoidal velocity, position tuning) | Position-tuning ramped the position at constant velocity `rate`, so its velocity was a rectangular pulse and the **acceleration was impulsive** at the corners (instantaneous demand → velocity-loop current saturation). Added **`test_max_accel` (0x2910:10, F32 RW, rad/s²)**: when `>0`, the position-tuning generator drives a **trapezoidal velocity profile** (accel at `max_accel` to cruise `rate`, cruise, decel to stop at `amplitude`; triangle if short) via the `v_allow=sqrt(2·a·dist)` braking law — motor accel bounded by `max_accel`, FF velocity follows the shaped ramp. `0`/`rate=0` → old linear/step behaviour. **Position tuning only** (scheduler passes 0 for velocity tuning, whose `rate` is already its accel). Trapezoidal chosen over S-curve. Additive `0x2910:10` non-PDO → version stays 3; CHANGELOG [3.11.0]; no CMC change. | ADR_032, include/mc_signal_gen.{h,c}, include/mc_od_store.h, src/mc_scheduler.c, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, gui |
| 031 | 2026-06-24 | Accepted | Velocity feedforward gain + signal-generator velocity (position-tuning FF) | The position cascade's velocity FF (`s_eff_vel_cmd = v_ff + vcorr`, `v_ff = trajectory velocity`) was a hardcoded 1:1 with no tuning knob, and position-tuning ran with `v_ff = 0`. Added **`velocity_ff_gain` (0x2200:4, F32 RW PERSIST, default 1.0)** applied as `s_eff_vel_cmd = velocity_ff_gain*v_ff + vcorr` (seeded 1.0 in LoadDefaults so 0/old-flash can't kill FF). Made `mc_signal_gen` emit its **velocity** (`vel` + `MC_SignalGen_Velocity()`: ±`rate` during ramp, 0 dwell/step/idle) so the generator is a `(position,velocity)` reference like the trajectory; position-tuning now uses `v_ff = generator velocity` through the same FF path (a position *step* stays pure feedback; a *ramp* feeds forward ±rate). Additive contract `0x2200:4`, CHANGELOG [3.9.0], **no version bump**, no CMC change. | ADR_031, include/mc_signal_gen.{h,c}, include/mc_od_store.h, src/mc_od.c, src/mc_scheduler.c, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, gui |
| 030 | 2026-06-24 | Accepted | Loop-tuning modes — motor-side test-signal overlay + shared signal generator | Velocity/position-loop tuning needs clean motor-generated reference signals. Added a motor-owned `test_mode` overlay (`0x2910:1`: 0 off / 1 velocity-tuning / 2 position-tuning) that, while the matching operational mode is **enabled**, redirects that loop's reference to an on-motor signal generator — velocity-tuning → `s_eff_vel_cmd`; position-tuning → position demand into the position controller, **bypassing the trajectory** (vel-FF 0), around the entry position. Overlay (not self-enabling): reuses the normal enable/safety/CMC drive-state; clearing it ramps bumplessly to 0. New pure `mc_signal_gen` module (ramp/pulse/step + continuous alternating; `rate=0`→step) reused for both domains. OD block `0x2910:1-7` (mode, amplitude, rate, dwell, continuous, trigger, active). Rejected: CiA-402 tuning modes (CMC routing/desync), self-enabling state (CMC desync), GUI staircase (too coarse), trajectory reuse (awkward). Additive contract, **no version bump**, no CMC change. | ADR_030, include/mc_signal_gen.h, src/mc_signal_gen.c, src/mc_scheduler.c, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, gui |
| 029 | 2026-06-24 | Accepted | Wire the over-current trip threshold to the OD (current_trip_a) | The only fault source is the **measured** over-current trip (`s_oc_trip` when `imax = max\|ia,ib,ic\| > g_mc_inject.current_limit_a`). `current_trip_a` (0x2600:2, RW PERSIST) was meant to set it but `od_mirror_live` overwrote it from the live value every medium tick → GUI writes had no effect (trip pinned at the 3.0 A init default, watch-window-only). Fix: `od_apply_gains` now applies `g_mc_inject.current_limit_a = max(g_od.current_trip_a, 0.1f)` (floor stops a stray 0 locking out the drive); removed the reverse mirror. Trip is now GUI-settable + PERSIST. **No contract/GUI change** (entry + Motor Config row already exist). Note: demanded iq is separately *clamped* to `vel_current_limit_a` (0x2300:4), not faulted — give the clamp↔trip pair headroom to avoid nuisance trips. | ADR_029, src/mc_scheduler.c, include/mc_debug.h, docs/spec/12_faults.md |
| 028 | 2026-06-22 | Accepted | D3 position execution — trajectory→position→velocity cascade | `PROFILE_POSITION` was set up by the CMC but the motor forced it safe-off ("not routed yet"). Implemented `mc_position_controller.c` (P-only default, gains `0x2200`, per spec 09: `velocity_correction = PID(position_error)`) and wired the scheduler cascade (medium 1 kHz, home-relative): `NEW_SETPOINT` starts a trapezoidal plan (target `0x607A`, time `0x607B`, limits `0x6081/3/4`); each tick trajectory → position loop → `velocity_demand = traj_vel_ff + correction` → velocity loop; `accel_ff` → torque inertia slot. `target_reached` (complete + `|err|<window`) → statusword `MC_IF_SW_TARGET_REACHED (0x0400)`. First cut: from-rest, single fixed window, no following-error fault. Completes D3 stages 2-5 (ADR-025). No contract change. | ADR_028, include/mc_position_controller.h, src/mc_position_controller.c, src/mc_scheduler.c, docs/spec/09_control_loops.md |
| 027 | 2026-06-22 | Accepted | PC command surface + current/torque mode via the CMC axis_manager | PC command page commands via `axis_manager` (`0x30xx`) per INTERFACE_SPEC §5b. Motor already routes `TORQUE_CURRENT` (`0x6060=4` + `0x6071` as amps); the CMC had no torque mode/target. Design: CMC-owned `0x302B axis_target_current` (F32, amps) + `MC_IF_AXIS_MODE_TORQUE (5u)`; axis_manager maps mode→`MC_IF_MODE_TORQUE` and SDO-writes current to `0x6071` (existing `SEQ_TARGET_TORQUE`); set-and-hold via SDO (v3 cyclic streams only velocity). Motor unchanged (already wired). **Per the user's direction, the contract additions + CMC implementation are delegated to the CMC via `Interface/REQUESTS.md` REQ-0012, NOT applied here** (no CHANGELOG bump until they implement). PC tool adds the control, shown pending REQ-0012. | ADR_027, ../Lightweight_CMC/Interface/REQUESTS.md (REQ-0012), gui |
| 026 | 2026-06-22 | Accepted | Calibration completeness reporting (OD bitfield) + wire current-offset cal command | New RO OD entry `0x2700:5 cal_done_flags` (U16) reports which calibrations have valid data: bit0 electrical / bit1 mech-zero / bit2 current-offset (`MC_IF_CAL_DONE_*`); a clear bit = outstanding. Wired the previously-dead `MC_IF_CAL_CURRENT_OFFSET (2)` cal command → triggers the existing `MC_CurrentSense_CalibrateOffsets` (fast loop, PWM-off, safety-gated on `!s_pwm_on`). Completeness **derived** from existing state (`electrical_offset≠0`, `home_offset≠0` per ADR-022, `s_cs.calibrated` with a save-time 0-sentinel so a restore can't falsely report it) → **no `MC_PARAM_STORE_VERSION` bump**, saved gains survive. `od_mirror_live` mirrors it each medium tick. GUI lists the *outstanding* ones + gains a Current-offset button. Contract: additive RO entry, CHANGELOG [3.5.0], **no protocol-version bump**; CMC forwards unchanged, PC tool displays. | ADR_026, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_od_store.h, src/mc_scheduler.c, docs/spec/13_calibration.md, gui |
| 025 | 2026-06-22 | Accepted | Trajectory planner — trapezoidal first cut (D3 stage 1) | First D3 stage, per spec 08/09. Trapezoidal planner with a fixed **1/6 : 2/3 : 1/6** (accel:cruise:decel) split, implemented against the pre-defined `mc_trajectory.h` interface (added `accel` to `MC_TrajSegment_t` so a step-accel trapezoid fits + generalises to the S-curve). From rest: `v_cruise=1.2D/T`, `a=7.2D/T²`, `T_min=max(1.2D/v_max, √(7.2D/a_lim))`; honours `requested_time` or stretches + reports. First cut: from-rest only (start vel accepted, treated 0), non-zero target vel/accel → `UNSUPPORTED_BOUNDARY`, jerk unused. Host-tested: 6 moves all bounded (vel/accel ≤ limits) + exact end + correct stretch. Next: position controller + scheduler integration (stages 2–5), then the S-curve. | ADR_025, include/mc_trajectory.h, src/mc_trajectory.c |
| 024 | 2026-06-22 | Accepted | OD-triggered electrical alignment (current-regulated open-loop) | Electrical alignment as one OD command. Added `0x2700:3 cal_align_current_a` (F32) + `0x2700:4 cal_align_hold_ms` (U16), both PERSIST; trigger `0x2700:1 = MC_IF_CAL_ALIGN_CAPTURE` (existing). Medium-loop state machine owns the command path: force electrical angle 0, slow Vd integrator regulates the d-axis current (= phase-A at angle 0) to the target (clamped under the trip), hold, capture `electrical_offset = wrap2π(−pos·pp)`, safe-off, save. **No FOC dependency** (forced angle + 1-D regulator, not the d/q PI). Idle-gated start, over-current abort, hold-bounded. `cal_status`: 1=running/0=done/0xFFFF=fault. Contract: CHANGELOG [3.2.0], **no protocol bump** (CMC recompiles). Verified: table 65 w/ `0x2700:3/4`, persistent gather 233 B. | ADR_024, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md, include/mc_od_store.h, src/mc_od.c, include/mc_debug.h, src/mc_scheduler.c |
| 023 | 2026-06-22 | Accepted | Persist OD gains — wire the `persistent` flag into the flash store | The `MC_IF_F_PERSIST` flag was descriptive-only (nothing read it); only `MC_CalibData_t` was saved, so gains reset to defaults on boot (user hit it: `vel_kp` didn't survive a power cycle). Wired it: `MC_Od_Gather/RestorePersistent` serialize every persistent OD entry `{index,sub,len,value}`; store payload `MC_CalibData_t`→`MC_Params_t` (calib + od_blob), version 1→2, max payload 256→512; `params_save` (was `calib_save`) gathers calib+gains, triggered by the SAVE command (`0x2800:1 = SAVE_MAGIC`); boot restores into `g_od`. **Explicit save** (no auto-save, to spare flash); flash write idle-gated as before. Consequence: version bump → existing calibration wiped once (re-cal after flash). Verified: host gather/restore round-trip PASS (219 B; floats + U16/U8). | ADR_023, src/mc_od.c, include/mc_od.h, include/mc_calib_data.h, include/mc_persistent_store.h, src/mc_scheduler.c |
| 022 | 2026-06-22 | Accepted | Mechanical home / "set mechanical zero" (multi-turn position reference) | Position commands need a user-set home. Added a multi-turn home offset (`s_home_offset_rad` = current absolute continuous position on capture); OD `position_actual` (0x6064) published home-relative (`mech − home`); persisted in calib (`reserved`→`home_offset_rad`, binary-compatible, no store-version bump). Triggered by new cal command `MC_IF_CAL_SET_MECH_ZERO (3)` on `0x2700:1` (wires the previously-dead `cal_command`) or watch-window inject; auto-saves. Contract: new cal code in `mc_if_od.h` + CHANGELOG [3.1.0], **no protocol version bump**; CMC forwards (no code change), PC tool adds the action. Caveat: single-turn encoder — home exact in-session, only within-1-rev recovers across reboot (re-home multi-turn axes). PROFILE_POSITION target relative once D3 lands. | ADR_022, include/mc_calib_data.h, include/mc_debug.h, src/mc_scheduler.c, ../Lightweight_CMC/Interface/mc_if_od.h + CHANGELOG.md |
| 021 | 2026-06-22 | Accepted | Adopt v3 cyclic command (streaming velocity_setpoint; joystick stays CMC-side) | Interface v3 ([3.0.0], REQ-0009/REQ-0010): cyclic command → 10 bytes `{controlword, velocity_setpoint, command_counter}`; mode/targets/profile params now **SDO-only** (they stick → host velocity control works end-to-end); `velocity_setpoint`→`0x60FF` is the live demand (loop consumes it, SDO write informational); `MC_IF_MODE_JOYSTICK_VELOCITY` removed (joystick is a CMC concept — REQ-0010 re-cut); `NEW_SETPOINT` rising edge decoded + latched (`ds.new_setpoint_latched`, consumed by D3); new OD `0x607B target_position_time_ms` (auto-generated, table 62→63); version 2→3 (auto). PROFILE_POSITION execution deferred to D3. Verified: syntax-clean + table dump. | ADR_021, src/mc_comms.c, include/mc_od_store.h, src/mc_mode_manager.{c,h}, src/mc_scheduler.c, docs/spec/05-07 |
| 020 | 2026-06-22 | Accepted | Generate motor OD table from the shared X-macro (owner-filtered) | `s_od_table[]` is now expanded from `MC_IF_OD_OBJECTS(X)` filtered to `MC_IF_OWNER_MOTOR`, bound to `g_od` by name → the contract is the single source of truth and drift is a **compile error** (`&g_od.<name>`); type/access cast (enum-aligned, `_Static_assert`), PDO/PERSIST from flags; CMC `0x3xxx` vanish; `0x2A00` skipped (mc_comms). Renamed `modes_display`→`modes_of_operation_display`; write-ranges patched in `MC_Od_Init` (table now RAM). Verified by diffing original vs generated table dumps: identical (62==62) except 4 `inject_*` `persistent` 1→0 (latent drift corrected; flag currently unused → behaviourally inert). Resolves ADR-019's drift risk. | ADR_020, src/mc_od.c, include/mc_od_store.h, src/mc_scheduler.c |
| 019 | 2026-06-22 | Accepted | Adopt Interface contract v2 (OD owner column + 0x3xxx CMC entries; protocol 1→2, CMC REQ-0008) | Shared contract bumped to v2: every OD X-macro entry gains an `owner` column, a new `0x3xxx` CMC-owned axis_manager block is added, `MC_IF_PROTOCOL_VERSION` 1→2 (wire layout byte-identical). Motor side needs **no functional change**: the OD table is hand-maintained (never expands `MC_IF_OD_OBJECTS`, so the owner column + `0x3xxx` are invisible to it); version is validated/stamped via the shared `MC_IF_PROTOCOL_VERSION` constant (rebuild → accept/emit v2, reject v1); an unknown `0x3xxx` index already returns `NO_OBJECT`. Host syntax-check clean. Deploy = coordinated v2 cutover (link down on any version mismatch). | ADR_019, src/mc_od.c (comment), docs/spec/05_object_dictionary.md, ../Lightweight_CMC/Interface/REQUESTS.md |
| 018 | 2026-06-21 | Accepted | Mode manager (E1) + commissioning-vs-remote arbitration | mc_mode_manager: simplified CiA-402 state machine (CW_ENABLE/QUICK_STOP/FAULT_RESET/HALT -> statusword + active mode). Scheduler arbiter: inject_enable -> commissioning (watch window, unchanged); else remote (OD/CiA-402 drives, boot-safe). Effective command consumed by fast/medium loops. mc_comms decoupled from the harness (writes/reads OD). | ADR_018, src/mc_mode_manager.c, src/mc_scheduler.c, src/mc_comms.c, docs/spec/07_mode_manager.md |
| 017 | 2026-06-21 | Accepted | CiA-402 OD entries + result codes + ERROR staging + cleanup (CMC REQ-0001/2/3/5/6) | Added 17 CiA-402 standard OD objects (RO actuals mirrored+scaled, RW stored for the mode manager); MC_OdStatus_t gains NO_SUB/NOT_READY with 1:1 wire mapping; 6 manufacturer entries; mc_comms stages ERROR on bad frame/unknown msg; removed stale mc_spi_protocol.h + MC_SPI_* defines. REQ-0004 (0x2A00 into OD) deferred. | ADR_017, src/mc_od.c, include/mc_od*.h, src/mc_comms.c, src/mc_scheduler.c, include/mc_config.h, ../Lightweight_CMC/Interface/REQUESTS.md |
| 016 | 2026-06-21 | Accepted | SPI inter-MCU protocol handler + SPI2-slave DMA (F2a+F2b) | mc_comms.c (HAL-free, on the shared headers): frame validate + dispatch (cyclic cmd apply, OD read/write via mc_od, 0x2A00 telemetry map + gather, pipelined responses); command dead-man (30 ms, master-gated). mc_spi_slave_stm32g474.c: SPI2 slave 64-byte full-duplex DMA, handler per transaction + re-arm, error recovery; armed from main.c. MC_Od_ReadRaw added. | ADR_016, src/mc_comms.c, include/mc_comms.h, src/mc_spi_slave_stm32g474.c, include/mc_spi_slave.h, src/mc_od.c, src/mc_scheduler.c, Core/Src/main.c |
| 015 | 2026-06-21 | Accepted | Object dictionary engine + table (motor side, F1) | mc_od.c engine (typed Find/Read/Write, access/type/size/range checks, callbacks) + mc_od_store g_od backing; tuning subset of the shared map (gains/telemetry/limits, float32 SI). Additive scheduler integration: seed at init, slow-loop apply OD gains to live controllers, medium-loop mirror telemetry. CiA-402 + 0x2A00 map deferred to the SPI transport. | ADR_015, src/mc_od.c, include/mc_od_store.h, src/mc_scheduler.c |
| 014 | 2026-06-21 | Accepted | Configurable runtime telemetry mapping + all-UDP network | Cyclic telemetry frame = fixed 12B header + host-configurable mapped blob (OD 0x2A00 TX-PDO map, runtime-reconfigurable, atomic swap + map_version, ~10 float32 in 64B). Network MCU exposes OD + telemetry to the PC over UDP only (OD req/resp w/ retransmit; fire-and-forget batched stream). Defined in the shared Interface package. | ADR_014, ../Lightweight_CMC/Interface/* (mc_if_od.h, mc_if_protocol.h, INTERFACE_SPEC.md, NETWORK_UDP_SPEC.md) |
| 013 | 2026-06-21 | Accepted | Shared inter-MCU interface contract (OD + SPI) | Boundary defined as a standalone dependency-free package at ../Lightweight_CMC/Interface (mc_if_protocol.h + mc_if_od.h + spec) shared by both firmwares. Redesigned framing: fixed 64-byte full-duplex frames, CRC16/Modbus, master=network/slave=motor, pipelined OD responses; completed OD resp/heartbeat/error payloads; 0x2xxx manufacturer objects = float32 SI; CiA-402 scaled-int. | ADR_013, ../Lightweight_CMC/Interface/*, docs/spec/06_spi_protocol.md, docs/spec/05_object_dictionary.md |
| 012 | 2026-06-21 | Accepted | Velocity loop + torque/current request (D2) | mc_velocity_controller (PI velocity->torque) + mc_current_request (torque+FF, clamp, iq=torque/Kt). Gains = proven loop * Kt (kp~34.65, ki 231; net iq identical). Runs at 1 kHz, publishes iq to the 20 kHz FOC (atomic). velocity_enable mode; feedback = observer velocity; accel/friction FF=0 for now; trip raised to 3 A. | ADR_012, src/mc_velocity_controller.c, src/mc_current_request.c, src/mc_scheduler.c, include/mc_debug.h, docs/spec/09_control_loops.md |
| 011 | 2026-06-19 | Accepted | FOC current loop (D1) | mc_pid.c (reusable PID) + mc_foc.c (Clarke/Park, d/q PI kp1.7/ki1700, circular V-limit, inverse Park, SVPWM min/max injection). Fast-loop FOC mode using the medium-loop electrical angle published as an atomic float; iq/id commanded from watch, over-current trip applies. Angle 1 ms stale (OK low speed); no dead-time comp yet. | ADR_011, src/mc_pid.c, src/mc_foc.c, include/mc_debug.h, src/mc_scheduler.c |
| 010 | 2026-06-21 | Accepted | Flash parameter persistence (calibration-only first) | Generic A/B NV store (mc_persistent_store + mc_flash_port, bank 2 pages 126/127, seq+crc32, verify-after-write) with MC_CalibData_t payload (electrical offset, current offsets, zero, phase order). Auto-save on alignment capture; written in slow loop when drive off; reload+apply on boot. Linker reserves top 4K. | ADR_010, include/mc_persistent_store.h, include/mc_flash_port.h, include/mc_calib_data.h, src/mc_persistent_store.c, src/mc_flash_port_stm32g474.c, src/mc_scheduler.c, STM32G474RETX_FLASH.ld |
| 009 | 2026-06-19 | Accepted | PWM backend + open-loop d-axis drive & alignment (C1/C2) | TIM1 PWM backend gated by MOE; open-loop Vd at commanded electrical angle (inverse Clarke, duty=0.5+V/Vbus); manual Vd ramp, gated by inject_enable, Vd/duty clamps + latched over-current trip; alignment capture sets electrical_offset so elec reads 0 at the held rotor. Bench PWM test removed. | ADR_009, src/mc_pwm_stm32g474.c, src/mc_math.c, include/mc_debug.h, src/mc_scheduler.c, Core/Src/main.c |
| 008 | 2026-06-19 | Accepted | Feedback path: SSI read, decode, state estimation | SPI1 16-bit Mode 2, 2x16-bit frame, position=bits[30:10] (21-bit); decode applies direction+zero offset; estimator does multi-turn continuous position, finite-diff+LPF velocity, electrical angle; read in 1 kHz medium loop. Observer (ADR-003) deferred to B2b. | ADR_008, include/mc_ssi_encoder.h, include/mc_state_estimator.h, src/mc_ssi_encoder*.c, src/mc_state_estimator.c, src/mc_scheduler.c, include/mc_debug.h |
| 007 | 2026-06-19 | Accepted | Current sensing: dual-ADC read, scaling, sign, offset cal | Dual regular-simultaneous CDR read (ADC1=A, ADC2=C, slave started first); i=(raw-offset)*sign*Vref/(fs*gain*shunt), sign=-1, ib=-(ia+ic); offset cal averages N samples at zero current via watch flag; scaling from board profile 0. | ADR_007, include/mc_current_sense.h, src/mc_current_sense_stm32g474.c, src/mc_board_config.c, include/mc_debug.h, src/mc_scheduler.c, Core/Src/main.c |
| 006 | 2026-06-19 | Accepted | Real-time scheduling & ISR dispatch | Fast 20 kHz from ADC end-of-conversion (ADC triggered by TIM1 TRGO=OC4REF at the PWM peak; sample-synchronised), medium 1 kHz from TIM7, slow 100 Hz decimated/serviced in main loop; HAL-free mc_scheduler + main.c dispatch. | ADR_006, include/mc_scheduler.h, src/mc_scheduler.c, src/mc_debug.*, Core/Src/main.c, docs/spec/04_realtime_scheduling.md |
